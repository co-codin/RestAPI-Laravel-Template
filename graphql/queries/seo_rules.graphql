extend type Query {
    seo_rules(
        where: _ @whereConditions(columns: ["id", "title", "url", "text", "assigned_by_id"])
        orderBy: _ @orderBy(columns: ["id", "title", "url", "assigned_by_id"])
    ): [SeoRule!]!
    @paginate
    @orderBy(column: "id", direction: "ASC")

    seo_rule(id: ID @eq): SeoRule @find
}
