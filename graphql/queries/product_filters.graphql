extend type Query {
    getProductFilters(
        filters: Mixed
        page: Mixed
        include: String
        fields: Mixed
        orderBy: String
    ): ProductFilter
    @field(resolver: "\\Modules\\Product\\Builder\\ProductFilterBuilder@getProductFilters")
}
