extend type Query {
    customer_reviews(
        where: _ @whereConditions(columns: ["id", "post", "author", "type", "is_home"])
        orderBy: _ @orderBy(columns: ["id", "post", "author", "type", "is_home"])
    ): [CustomerReview!]!
    @paginate
    @orderBy(column: "id", direction: "ASC")

    customer_review(id: ID @eq): CustomerReview @find
}
