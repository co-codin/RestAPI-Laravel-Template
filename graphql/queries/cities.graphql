extend type Query {
    cities(
        where: _ @whereConditions(columns: ["id", "region_name", "region_name_with_type", "city_name", "city_slug"])
        orderBy: _ @orderBy(columns: ["id", "region_name", "region_name_with_type", "city_name", "city_slug"])
    ): [City!]!
    @paginate(defaultCount: 15, maxCount: 200)
    @orderBy(column: "id", direction: "ASC")

    city(id: ID @eq): City @find
}
