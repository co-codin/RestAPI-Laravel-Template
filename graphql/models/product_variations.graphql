type ProductVariation {
    id: ID!
    product_id: Int!
    name: String!
    price: Float
    previous_price: Float
    currency_id: Int
    is_price_visible: Boolean!
    is_enabled: Boolean!
    availability: Int!
    stock_type: String

    currency: Currency
}
