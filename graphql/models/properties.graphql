type Property {
    id: ID!
    name: String
    type: Int!
    options: String
    description: String
    is_hidden_from_product: Boolean!
    is_hidden_from_comparison: Boolean!
    filters: [Filter!] @hasMany
}
